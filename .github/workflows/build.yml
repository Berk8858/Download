name: Build Windows EXE

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install pyinstaller yt-dlp
      
      - name: Download ffmpeg
        run: |
          Invoke-WebRequest -Uri "https://github.com/BtbN/FFmpeg-Builds/releases/download/latest/ffmpeg-master-latest-win64-gpl.zip" -OutFile "ffmpeg.zip"
          Expand-Archive -Path "ffmpeg.zip" -DestinationPath "ffmpeg"
          Copy-Item "ffmpeg\ffmpeg-master-latest-win64-gpl\bin\ffmpeg.exe" -Destination "."
      
      - name: Download yt-dlp.exe
        run: |
          Invoke-WebRequest -Uri "https://github.com/yt-dlp/yt-dlp/releases/latest/download/yt-dlp.exe" -OutFile "yt-dlp.exe"
      
      - name: Build EXE
        run: |
          python -m PyInstaller --onefile --noconsole --name Multi-Downloader --icon download-icon.ico --add-data "yt-dlp.exe;." --add-data "ffmpeg.exe;." --hidden-import yt_dlp --collect-submodules yt_dlp --clean --noconfirm twitter-indirici.py
      
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: Multi-Downloader
          path: dist/Multi-Downloader.exe
      
      - name: Create Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: dist/Multi-Downloader.exe
          draft: false
          prerelease: false
